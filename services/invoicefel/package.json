{
  "name": "cabisa-api",
  "version": "1.0.1",
  "description": "API Services",
  "main": "handler.js",
  "author": "Cesar Salazar",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "export AWS_PROFILE=cabisa && serverless deploy",
    "sls-local": "nodemon --exec sls offline"
  },
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.467.0",
    "lambda-warmer": "^1.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.18",
    "serverless-offline": "^8.8.1"
  }
}
